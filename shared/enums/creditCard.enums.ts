/** @summary Supported credit card flags. */
export enum CreditCardFlag {
    VISA = "visa",
    MASTERCARD = "mastercard",
    AMEX = "amex",
    ELO = "elo",
    HIPERCARD = "hipercard",
    DISCOVER = "discover",
    DINERS = "diners",
}
