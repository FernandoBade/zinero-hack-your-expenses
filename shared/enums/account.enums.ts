/** @summary Supported account types. */
export enum AccountType {
    CHECKING = "checking",
    PAYROLL = "payroll",
    SAVINGS = "savings",
    INVESTMENT = "investment",
    LOAN = "loan",
    OTHER = "other",
}
